<% trs = escape_javascript(render partial: "category",
                                  locals:  { categories: @categories,
                                             name:       t(".name"),
                                             use:        t(".use") }) %>

document.getElementById('listing').insertAdjacentHTML('beforeend','<%= trs %>');
