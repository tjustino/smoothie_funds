<%= breadcrumb link_to(@current_account.name, account_categories_path(@current_account)),
               link_to(t(".edit_category"), edit_category_path(@category)) %>
<div class="columns is-centered">
  <div class="column is-narrow">
    <%= render partial: "form", locals: { title: t(".edit_category") } %>
  </div>
</div>
